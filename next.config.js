/** @type {import('next').NextConfig} */
const nextConfig = {}

module.exports = {
    images: {
      domains: ['firebasestorage.googleapis.com'],
      // ... other configurations
    },
    // ... rest of the config
  }
  
